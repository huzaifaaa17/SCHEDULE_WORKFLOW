name: SCHEDULE_WORFLOW

on:
  workflow_dispatch: 
  schedule:
    - cron: "0 0 * * *"

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps: 
      - name: Print Message
        run: echo "Schedule workflow Executed"
